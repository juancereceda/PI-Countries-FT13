import styled from "styled-components";

const Div = styled.div`
  display: flex;
  .formContainer {
    background-color: gray;
    padding-right: 1rem;
    margin-right: 3rem;
    margin-bottom: 3rem;
    .searchForm {
      display: flex;
      flex-direction: column;
    }
  }
`;

export default Div;
